# UI Tests

UI test sources live here for the generated `TextCalcMenuBar.xcodeproj`.

Current scenarios:

- popup opens from menu bar
- representative natural-language sum prompt returns `28`
- app launches in `-ui-testing` mode so hotkey registration is skipped during UI automation
